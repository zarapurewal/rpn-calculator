#!/usr/bin/env zsh

mods=( "chai" "chai-http" "coffeescript" "express" "express-static" "mocha" "nodemon" )

for mod in $mods
do
  if ! [[ -d node_modules/$mod ]]
  then
    exec npm install
    exit $?
  fi
done

true
