These two demos illustrate that, while creating custom images is
one way to work with docker, it is also possible to create and then
customise a live image:

1. Mapping a (data) volume into a container.
2. Loading an SQL database.
